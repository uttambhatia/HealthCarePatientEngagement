apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-appointment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: svc-appointment
  template:
    metadata:
      labels:
        app: svc-appointment
    spec:
      containers:
        - name: svc-appointment
          image: ghcr.io/uttambhatia/healthcarepatientengagement/svc-appointment:latest
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: aks
            - name: SERVICEBUS_NAMESPACE
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: servicebus-namespace
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
