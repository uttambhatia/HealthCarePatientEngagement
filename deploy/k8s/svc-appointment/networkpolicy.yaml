apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: svc-appointment-ingress
spec:
  podSelector:
    matchLabels:
      app: svc-appointment
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: api-gateway
      ports:
        - protocol: TCP
          port: 8082
