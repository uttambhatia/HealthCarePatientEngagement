apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: svc-notification-ingress
spec:
  podSelector:
    matchLabels:
      app: svc-notification
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: api-gateway
      ports:
        - protocol: TCP
          port: 8086
