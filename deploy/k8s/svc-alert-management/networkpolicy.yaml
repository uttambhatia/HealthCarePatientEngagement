apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: svc-alert-management-ingress
spec:
  podSelector:
    matchLabels:
      app: svc-alert-management
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: api-gateway
      ports:
        - protocol: TCP
          port: 8089
